# ============================================================
# CIROCHAT — Variables de entorno
# Copia este archivo como .env y rellena los valores reales
# ============================================================

# Base de datos PostgreSQL (con extensión pgvector)
DATABASE_URL="postgresql://USUARIO:PASSWORD@HOST:PUERTO/NOMBRE_DB"

# NextAuth — genera un secreto con: openssl rand -base64 32
NEXTAUTH_SECRET="CAMBIA_ESTO_POR_UN_SECRETO_ALEATORIO"
NEXTAUTH_URL="https://tudominio.com"

# Encriptación AES-256 para API keys de clientes
# Exactamente 32 caracteres — genera con: openssl rand -hex 16
ENCRYPTION_KEY="CADENA_ALEATORIA_DE_EXACTAMENTE_32__"

# Email para envío de notificaciones de leads (opcional)
EMAIL_HOST="smtp.gmail.com"
EMAIL_PORT="587"
EMAIL_USER="tucuenta@gmail.com"
EMAIL_PASS="tu_contrasena_de_aplicacion"
EMAIL_FROM="Cirochat <noreply@tudominio.com>"
